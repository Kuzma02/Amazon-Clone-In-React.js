import React from 'react';

const SimpleSliderCard = ({sliderElement}) => {
    return (
      <div>
      <img src={ sliderElement.image } alt="" />  
      </div>

    )
}

export default SimpleSliderCard;